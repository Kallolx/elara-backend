# Server Configuration
PORT=5000
NODE_ENV=development
BACKEND_URL=http://localhost:5000

# PostgreSQL Connection String (Format for Prisma)
# Replace user, password, host, port, and dbname with your local Postgres credentials
DATABASE_URL="postgresql://username:password@localhost:5432/elara_db?schema=public"
